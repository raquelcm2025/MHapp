<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:allowBackup="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyHobbiesApp">

        <activity android:name=".ui.activity.AccesoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.InicioActivity"
            android:exported="true" />
        <activity android:name=".ui.activity.RegistroActivity"
            android:exported="true"/>

        <activity android:name=".ui.activity.ListaHobbiesActivity"
            android:exported="false"/>
        <activity android:name=".ui.activity.ChatActivity"
            android:exported="false" />

        <activity android:name=".ui.activity.HistorialActivity"
            android:exported="false"/>

    <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"/>
    </application>
</manifest>